-hacer todos los algoritmos en clases para poder mantener la llave creada, en modo se inicializa una instancia, y luego esa instancia cifra las cosas, para poder usar la lib para implementar cosas eficientes, q no tengan q recalcular la clave
-salsa20, RC4 revisar los sizes de key and nonce, poner alguna advertencia o algo, cuando estan mas grandes de lo normal, y/o quiza permitir hashearlos con salsa mismo para obtener un key/nonce del tamanno requerido(aunq no se si esto ultimo introduzca vulnerabilidades)
-no compila en msvc por un detalle en salsa20 q uso cpp20
-line 30 RC4, need to check if keylength is not 0, otherwise KAPUT
-hexstring2asciistring explodes when passed non hex string
-in RC4, RC4::KSA, start using unsigned char* instead of string, for performance and more compatibility with c ABI
-make some comparations with other implementations of algorithms